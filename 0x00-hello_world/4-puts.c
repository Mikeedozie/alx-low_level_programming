#include <stdio.h>

/**
 * main - This holds the code that prints a quote,
 * description: this block exits after the message prints
 * Return: This ends the function block
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
