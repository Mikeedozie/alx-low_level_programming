#include <stdio.h>

/**
 * main - This returns the name of the program
 * @argc: Argument count
 * @argv: Argument Vector
 * Return: (0).
 */


int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
